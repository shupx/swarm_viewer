{
  "name": "swarm-viewer",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "main-app",
    "sub-app-demo"
  ],
  "scripts": {
    "dev": "./start.sh",
    "build": "npm run build -w sub-app-demo && npm run build -w main-app",
    "lint": "npm run lint -w main-app && npm run lint -w sub-app-demo"
  }
}
