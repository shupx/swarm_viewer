{
  "name": "swarm-viewer",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "./start.sh",
    "build": "pnpm --filter sub-app-demo build && pnpm --filter main-app build",
    "lint": "pnpm --filter main-app lint && pnpm --filter sub-app-demo lint"
  }
}
